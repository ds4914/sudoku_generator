## 1.0.0

- Initial release of Flutter Remote UI.
- Core rendering engine (`RemoteUIRenderer`).
- Standard widget set: `Column`, `Row`, `Stack`, `Text`, `Image`, `Button`.
- Form support with `TextInput` and validation rules.
- Conditional logic support with expressions.
